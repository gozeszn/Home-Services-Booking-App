NODE_ENV=development
PORT=5000
MONGODB_URI=your_mongodb_connection_string_here
JWT_SECRET=replace_this_with_a_random_secret
JWT_EXPIRES_IN=1h
CLIENT_ORIGIN=http://localhost:5173

# Optional: required only when running the development seed script.
# Generate your own password (12+ characters, at most 72 UTF-8 bytes).
SEED_PASSWORD=
